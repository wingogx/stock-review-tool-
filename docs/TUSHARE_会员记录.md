# Tushare会员记录

## 📋 当前会员信息

| 项目 | 信息 |
|------|------|
| **账号Token** | `4937369187852746524` |
| **账号类型** | 1万积分会员 |
| **购买日期** | 2025-12-11 |
| **有效期** | 7天 |
| **到期日期** | **2025-12-18** |
| **自定义API URL** | `http://7d01.xiximiao.com/dataapi` |

## ⚠️ 重要提醒

**如果在 2025-12-18 之后出现以下情况，很可能是会员已到期：**

1. 接口返回错误：`抱歉，您没有访问权限` 或 `积分不足`
2. `limit_list_ths()` 接口调用失败
3. 数据采集覆盖率突然下降

## 🔧 到期后的处理方案

### 方案1：续费会员（推荐）
- 重新购买1万积分会员
- 获取新的Token
- 更新 `.env` 文件中的 `TUSHARE_TOKEN`

### 方案2：降级使用免费/低积分接口
系统会自动fallback到：
1. **Tushare免费接口** (`concept_detail` - 5000积分)
2. **AKShare接口** (免费，但可能被反爬虫封锁)
3. **数据库历史数据** (备用)

## 📊 当前已使用的高级接口

以下接口需要1万积分权限，到期后将无法使用：

| 接口 | 用途 | 重要性 |
|------|------|--------|
| `limit_list_ths()` | 获取涨停池数据（60只） | ⭐⭐⭐ 高 |
| 其他10000积分接口 | 待补充 | - |

## 🔄 更新Token的步骤

1. 编辑 `.env` 文件
2. 更新 `TUSHARE_TOKEN=新的token`
3. 更新本文档的"当前会员信息"
4. 重启服务：
   ```bash
   # 重启后端服务
   cd backend
   # 如果使用systemd或pm2，重启对应服务
   ```

## 📝 历史记录

| 购买日期 | Token | 有效期 | 到期日期 | 状态 |
|---------|-------|--------|---------|------|
| 2025-12-11 | 4937369187852746524 | 7天 | 2025-12-18 | ✅ 使用中 |

---

**最后更新时间**: 2025-12-11
**维护者**: 系统自动生成
